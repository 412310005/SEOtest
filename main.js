/**
 * 單字庫前端腳本說明（本 repo 無獨立 main.js 入口）：
 * - glossary.js：單字卡 iframe（拖放、展開、scrollIntoView、高亮）。
 * - global_glossary_search.js：頂端固定搜尋 iframe（全章節索引、點選後以 URL query 觸發 Streamlit 切章／切分頁）。
 * 請執行 streamlit run app.py 使用。
 */
